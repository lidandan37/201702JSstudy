瀑布流 

假如说 有4列（ul）

每次在插入图片之前 通过sort方法 将这4列（ul）根据目前自身高度（offsetHeight） 从小到大排序

排序后 将要插入的图片放到 排完序后集合中第一项ul（也就是高度最小的那一列）里即可

1.获取数据 （ajax请求）
2.绑定数据（动态创建 ul排序）


3.延迟加载 
4.滚动条快滚到底时继续加载数据

结构 最外层容器一个div

四列 每一列是一个ul

每一列 中的每一项是一个li
每个li里 一个采集a标签 一个图片img标签 一个文本p标签

